services:
  - type: web
    name: pfms-filter
    env: python
    region: ohio
    plan: free
    buildCommand: "pip install -r PFMS-Filter-main/requirements.txt"
    startCommand: "gunicorn app:app"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
