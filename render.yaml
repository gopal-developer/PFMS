services:
  - type: web
    name: pfms-filter
    env: python
    region: ohio
    plan: free
    rootDir: PFMS-Filter-main
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
